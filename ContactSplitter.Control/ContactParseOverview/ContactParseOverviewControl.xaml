<UserControl x:Class="ContactSplitter.Control.ContactParseOverview.ContactParseOverviewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ContactSplitter.Control.ContactParseOverview"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             d:DataContext="{d:DesignInstance Type=local:ContactParseViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid>
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal" Margin="10">
                <TextBox IsReadOnly="True" IsEnabled="{Binding Enabled}" Margin="0,0,5,0" Text="Unformatierte Eingabe"></TextBox>
                <TextBox Text="{Binding Input}" Width="840" Height="40"></TextBox>
                <Button Command="{Binding ParseCommand}" Content="Parse" Margin="5,0,5,0" Width="120"></Button>
                <Button Command="{Binding SaveCommand}" Content="Save" Width="120"></Button>
            </StackPanel>
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="0,10,0,10" />
            <StackPanel Orientation="Horizontal" Margin="10">
                <StackPanel Orientation="Vertical">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,30">
                        <TextBox IsReadOnly="True" IsEnabled="{Binding Enabled}" Text="Anrede" Margin="0,0,5,0" Width="102"></TextBox>
                        <TextBox Text="{Binding Salutation}" Width="495"></TextBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,30">
                        <TextBox IsReadOnly="True" IsEnabled="{Binding Enabled}" Text="Brief-Anrede" Margin="0,0,5,0" Width="102"></TextBox>
                        <TextBox  Text="{Binding LetterSalutation}" Width="495"></TextBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBox IsReadOnly="True" IsEnabled="{Binding Enabled}" Text="Titel" Margin="0,0,5,0" Width="102"></TextBox>
                        <TextBox  Text="{Binding Title}" Width="495"></TextBox>
                    </StackPanel>
                </StackPanel>
                <StackPanel Orientation="Vertical" Margin="100,0,0,0">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,30">
                        <TextBox IsReadOnly="True" IsEnabled="{Binding Enabled}" Text="Geschlecht" Margin="0,0,5,0" Width="102"></TextBox>
                        <TextBox  Text="{Binding Gender}" Width="450"></TextBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,30">
                        <TextBox IsReadOnly="True" IsEnabled="{Binding Enabled}" Text="Vorname" Margin="0,0,5,0" Width="102"></TextBox>
                        <TextBox  Text="{Binding ForeName}" Width="450"></TextBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,40">
                        <TextBox IsReadOnly="True" IsEnabled="{Binding Enabled}" Text="Nachname" Margin="0,0,5,0" Width="102"></TextBox>
                        <TextBox  Text="{Binding LastName}" Width="450"></TextBox>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
            <StackPanel  Orientation="Horizontal" Margin="10">
                <TextBox IsReadOnly="True" Text="Nicht geparste Wörter" IsEnabled="{Binding Enabled}"></TextBox>
                <TextBox  Text="{Binding NotParsed}" Margin="5,0,0,0" Width="435"></TextBox>
            </StackPanel>
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="0,10,0,10" />
            <StackPanel Orientation="Horizontal" Margin="10">
                <TextBox IsReadOnly="True" Text="Anmerkungen" IsEnabled="{Binding Enabled}" Width="120"></TextBox>
                    <TextBox IsReadOnly="True" Width="1140" 
                             Text="{Binding Note}" AcceptsReturn="True" TextWrapping="Wrap" Margin="5,0,0,0">
                    </TextBox>
            </StackPanel>
            <ui:LoadingScreen HorizontalAlignment="Center" Background="Transparent" Margin="0,30,0,0" Visibility="{Binding IsLoading}"></ui:LoadingScreen>
        </StackPanel>
    </Grid>
</UserControl>
