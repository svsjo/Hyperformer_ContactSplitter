<UserControl x:Class="ContactSplitter.Control.ContactList.ContactListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ContactSplitter.Control.ContactList"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             d:DataContext="{d:DesignInstance Type=local:ContactListViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    
    <Grid Background="#355A61">
        <StackPanel Orientation="Vertical">
            <ui:DataGrid ItemsSource="{Binding ContactsView}" AutoGenerateColumns="False" Height="625">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#355A61"></Setter>
                        <Setter Property="FontWeight" Value="Bold"></Setter>
                        <Setter Property="Height" Value="30"></Setter>
                        <Setter Property="Margin" Value="0,10,0,0"></Setter>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Width="245" Header="Anrede" Binding="{Binding Salutation, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Width="250" Header="Brief-Anrede" Binding="{Binding LetterSalutation, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Width="240" Header="Titel" Binding="{Binding Title, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Width="100" Header="Geschlecht" Binding="{Binding Gender, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Width="220" Header="Vorname" Binding="{Binding ForeName, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Width="220" Header="Nachname" Binding="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" />
                </DataGrid.Columns>
            </ui:DataGrid>
            
            <StackPanel Orientation="Horizontal" Height="40" Margin="10,10,0,0">
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="250" Focusable="True"/>
            </StackPanel>
            
        </StackPanel>
    </Grid>
</UserControl>
