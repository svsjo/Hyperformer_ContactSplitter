<controls:UiWindow x:Class="ContactSplitter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ContactSplitter"
        xmlns:control="clr-namespace:ContactSplitter.Control;assembly=ContactSplitter.Control"
        xmlns:contactList="clr-namespace:ContactSplitter.Control.ContactList;assembly=ContactSplitter.Control"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Contact Splitter" Width="1360" Height="800"
        xmlns:pages="clr-namespace:ContactSplitter.Control;assembly=ContactSplitter.Control"
        xmlns:controls="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:contactParseOverview="clr-namespace:ContactSplitter.Control.ContactParseOverview;assembly=ContactSplitter.Control"
        xmlns:settings="clr-namespace:ContactSplitter.Control.Settings;assembly=ContactSplitter.Control"
        Background="{ui:ThemeResource ApplicationBackgroundBrush}"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid
        x:Name="RootMainGrid"
        Grid.Row="1"
        Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!--  This is the main navigation of the application.  -->
            <ui:NavigationStore
            x:Name="RootNavigation"
            Grid.Column="0"
            Margin="6,0,6,0"
            Frame="{Binding ElementName=RootFrame, Mode=OneWay}"
            SelectedPageIndex="0">
                <ui:NavigationStore.Items>
                    <ui:NavigationItem
                        Content="Parser"
                        Icon="BoxArrowLeft20"
                        PageType="{x:Type contactParseOverview:ContactParseOverviewControl}"
                        PageTag="parser" />
                    <ui:NavigationItem
                        Content="Adressbuch"
                        Icon="BookGlobe24"
                        PageType="{x:Type contactList:ContactListControl}" 
                        PageTag="list" />
                </ui:NavigationStore.Items>
                <ui:NavigationStore.Footer>
                    <ui:NavigationItem
                    Content="Einstellung"
                    Icon="ContentSettings16"
                    PageType="{x:Type settings:SettingsControl}" 
                    PageTag="settings" />
                </ui:NavigationStore.Footer>
            </ui:NavigationStore>

            <!--  We display our pages inside this element.  -->
            <Border
            Grid.Column="1"
            Background="{ui:ThemeResource ControlFillColorDefaultBrush}"
            CornerRadius="8,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Frame x:Name="RootFrame" Grid.Row="1" />
                    <ui:Breadcrumb
                    Grid.Row="0"
                    Margin="18"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    FontSize="24"
                    Navigation="{Binding ElementName=RootNavigation, Mode=OneWay}" />
                </Grid>
            </Border>
        </Grid>

        <!--  The title bar contains window navigation elements and some Tray related extras.  -->
        <!--  You can put additional controls in the header, such as a search bar.  -->
        <!--  <ui:TitleBar.Header />  -->
        <ui:TitleBar
      Title="Adressbuch Parser"
      Grid.Row="0">
            <ui:TitleBar.Tray>
                <ui:NotifyIcon
          FocusOnLeftClick="True"
          MenuOnRightClick="True"
          TooltipText="WPF UI">
                    <ui:NotifyIcon.Menu>
                        <ContextMenu>
                            <ui:MenuItem
                 Header="Home"
                 SymbolIcon="Library28"
                 Tag="home" />
                        </ContextMenu>
                    </ui:NotifyIcon.Menu>
                </ui:NotifyIcon>
            </ui:TitleBar.Tray>
        </ui:TitleBar>
    </Grid>
</controls:UiWindow>
